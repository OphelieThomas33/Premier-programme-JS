{
    "configurations": [
        {
            "type": "node",
            "name": "Terminal de débogage de JavaScript",
            "request": "launch",
            "cwd": "${workspaceFolder}"
        }
    ]
}